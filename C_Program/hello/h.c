#include<stdio.h>
#include<math.h>
#include <random>
int main(){
	int i,j=0;
	for(i=0;i<1;i++){
		printf("I love you.\n");
	}
	int dist;
	int r=StdRandom.discrete(dist);
	printf("%d\t",r);

	//double cx[][]

	//double x0 = cx[r][0]*x +cx[r][1]*y + cx[r][2];
	//double y0 = cy[r][0]*x +cy[r][1]*y + cy[r][2];
	//x=x0;
	//y=y0;

	//StdDraw.point(x,y);

	//if(t%100=0){
	//	StdDraw.show();
	//	StdDraw.pause(10);
	//}`
}
